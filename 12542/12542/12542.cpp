#include <iostream>
#include <string>
#include <sstream>
#include <iomanip> 
#include <math.h>

using namespace std;

string line;
int main()
{
	int i, j, k;
	while (cin >> line && line!="0") {
		for (i = 0; i < line.length();i++) {
			int con;
			for (j = line.length(); j >= i; j--) {
				
			}
		}
	}
	return 0;
}
